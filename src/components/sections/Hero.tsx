import GallerySection from "../GallerySection";

const Hero = () => {
  return (
    <div className="bg-gradient-to-r from-[#021d3d] to-[#043d70]">
      <GallerySection />
    </div>
  );
};

export default Hero;
