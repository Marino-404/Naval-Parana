import CardServices from "../ui/CardServices";

const ServicesSection = () => {
  return (
    <div className=" z-10 w-full h-[100vh] xl:h-[20vh] bg-primary">
      <CardServices />
    </div>
  );
};

export default ServicesSection;
